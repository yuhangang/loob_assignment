# Database Configuration
DB_USER=root
DB_PASSWORD=password
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=loob_unified

# Optional: Full DSN (takes precedence if set)
# DATABASE_DSN=root:password@tcp(127.0.0.1:3306)/loob_unified?charset=utf8mb4&parseTime=True&loc=Local

# API Configuration
HTTP_ADDR=:8080
PUBLIC_BASE_URL=http://localhost:8080
MOCK_GATEWAY_SECRET=change-me-local-only

# Redis Configuration
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# AWS Configuration (Local Mocking)
AWS_REGION=ap-southeast-1
SQS_QUEUE_URL=http://localhost:4566/000000000000/orders-queue
